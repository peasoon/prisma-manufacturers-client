import * as React from "react";

interface IFooterProps {}

const Footer: React.FunctionComponent<IFooterProps> = (props) => {
  return <footer className="footer">2023 год</footer>;
};

export default Footer;
